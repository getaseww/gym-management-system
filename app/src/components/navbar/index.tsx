import React, { FC } from 'react';

const Navbar:FC = () => {
  return (
    <nav className="bg-blue-500 p-4">
      <div className="container mx-auto text-white font-bold">Admin Dashboard</div>
    </nav>
  );
};

export default Navbar;
